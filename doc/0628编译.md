# 0628 编译存档

> 记录时间：2026-06-28

---

## 一、iOS 客户端

| 项目 | 内容 |
|------|------|
| **本地分支** | `fix/chat-session-routing` |
| **最新 commit** | `fcf7964` fix: enable Kochava ATT module so install goes to normal endpoint with IDFA |
| **连接服务器** | 测试服 `http://34.74.255.48`（`useTestServer = true`，DEBUG/Release 均为测试服） |
| **Xcode Scheme** | `WorkSurvivalGuide`（无 DEV 角标） |
| **编译配置** | Debug（Xcode 直接运行）/ Release（Archive 打包） |

### 最近 5 条 commit
```
fcf7964 fix: enable Kochava ATT module so install goes to normal endpoint with IDFA
a9e0128 chore: remove ATT diagnostic logs — production-ready
df77ccb fix: trigger ATT from scenePhase=.active instead of onAppear
eee7206 debug: add ATT diagnostic logs to pinpoint why ATT dialog never shows
9c586f4 feat: integrate Kochava SDK v10.0.2 for TikTok SKAdNetwork attribution
```

### AppConfig.swift 关键配置
```swift
var useTestServer: Bool {
#if DEBUG
return true  // 测试服
    #else
    return true   // ⚠️ 内测包临时改为测试服，提审前必须改回 false
    #endif
}
// readBaseURL = "http://34.74.255.48/api/v1"
// writeBaseURL = "http://34.74.255.48/api/v1"
// sseBaseURL   = "https://test-api.yohomie.art"
```

---

## 二、测试服务器（34.74.255.48）

| 项目 | 内容 |
|------|------|
| **服务器 IP** | `34.74.255.48` |
| **SSH 别名** | `ssh gemini-server` |
| **服务状态** | `active (running)` 自 2026-06-28 12:31:45 UTC |
| **代码来源** | 本地 `fix/chat-session-routing` 分支部署（无 git，通过 scp 部署） |
| **assistant.py 时间戳** | Jun 22（上次部署时间） |
| **数据库** | `gemini_audio_db_test` |
| **R2 Bucket** | `micink-assets-test` |
| **JWT_SECRET_KEY** | `test-secret-2026-ceshi` |
| **API_PUBLIC_URL** | `http://34.74.255.48` |
| **nginx** | HTTP 直通（port 80），无 HTTPS 重定向 |

---

## 三、正式服务器（34.74.150.225 / api.yohomie.art）

| 项目 | 内容 |
|------|------|
| **服务器 IP** | `34.74.150.225` |
| **域名** | `api.yohomie.art`（Cloudflare → nginx HTTPS） |
| **SSH 命令** | `ssh -i ~/.ssh/id_rsa liudanmi_gmail_com@34.74.150.225` |
| **服务状态** | `active (running)` 自 2026-06-28 11:03:09 UTC |
| **assistant.py 时间戳** | Jun 28（今日部署，含 chat session 功能） |
| **数据库** | `gemini_audio_db` |
| **R2 Bucket** | `micink-assets` |
| **JWT_SECRET_KEY** | `bbWw5TbmojoBmECcv8GkXBnjbOwRiNpNVVMx4uVEK4c-v1eePIdzClQ0lnoXN3dj` |
| **API_PUBLIC_URL** | `https://api.yohomie.art` |
| **nginx** | HTTPS（443，Cloudflare Origin Certificate），HTTP → HTTPS 重定向 |

---

## 四、当前已知问题

| 问题 | 状态 |
|------|------|
| iOS Simulator 连不上测试服 | 已定位：Mac 系统代理（127.0.0.1:7897）拦截了 Simulator 的 HTTP 请求。解决方案：在 System Preferences → Network → Proxies → Bypass 中添加 `34.74.255.48` |
| 苹果账号登录 token 失效 | 因今日更改测试服 JWT_SECRET_KEY，旧 token 全部失效，需重新登录 |

---

## 五、数据库对照

| 环境 | 数据库名 | R2 Bucket |
|------|----------|-----------|
| 测试 | `gemini_audio_db_test` | `micink-assets-test` |
| 正式 | `gemini_audio_db` | `micink-assets` |
