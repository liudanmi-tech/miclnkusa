# ============================================
# 直接启动服务 - 分步执行
# ============================================

# 步骤1：进入项目目录并启动服务（前台运行，测试用）
cd /home/admin/gemini-audio-service && python3 main.py

# 如果步骤1能正常启动（看到服务运行信息），按 Ctrl+C 停止
# 然后执行步骤2：后台启动

# ============================================
# 步骤2：后台启动服务（如果步骤1成功）
# ============================================
# cd /home/admin/gemini-audio-service && nohup python3 main.py > ~/gemini-service.log 2>&1 &

# ============================================
# 步骤3：验证服务（如果步骤2执行了）
# ============================================
# sleep 3 && curl http://localhost:8001/health

