# ============================================
# 修复 Gemini API 连接问题
# 执行用户: admin
# 执行路径: /home/admin/gemini-audio-service
# ============================================

# 步骤1：检查 .env 文件配置
cd /home/admin/gemini-audio-service && cat .env | grep -E "GEMINI|PROXY|USE_PROXY"

# 步骤2：测试 Gemini API 连接（使用 test-gemini 接口）
curl http://localhost:8001/test-gemini

# 步骤3：查看详细的错误日志
tail -100 ~/gemini-service.log | grep -E "Connection refused|连接|代理|proxy|gemini" | tail -20

