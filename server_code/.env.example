# 复制为 .env 并填写（不要提交 .env 到 git）
# 从服务端复制 GEMINI_API_KEY 到本地，或 scp 服务端的 .env

GEMINI_API_KEY=

# 数据库（本地可默认；RDS 需使用内网地址）
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/gemini_audio_db
# 阿里云 RDS 要求 SSL，连接 RDS 时设置 DATABASE_SSL=true
# DATABASE_SSL=true

# 代理（国内环境）
PROXY_URL=http://47.79.254.213/secret-channel
USE_PROXY=true
PROXY_FORCE_LOCALHOST=true
